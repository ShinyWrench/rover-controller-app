name: rover_controller_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
    sdk: '>=2.12.0 <3.0.0'

dependencies:
    flutter:
        sdk: flutter
    cupertino_icons: ^1.0.2
    flutter_bluetooth_serial: ^0.2.2
    simple_logger: ^1.8.1

dev_dependencies:
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
